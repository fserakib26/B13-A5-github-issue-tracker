1️⃣ What is the difference between var, let, and const?

Ans: var:- var is old system for variable decleration, It has some disadvantage to declare variable multiple times. var is functioned scoped thats why maximum times it has raised unexpected bugs.
    let:- let is the modern varible decleration system, it can be reassign value under the same variable name but it's unable to declare the same variable name for multiple times. It's deployed after release ES6
    const:- It can't change it's value. So, this is one kinds of the variable decleration system which includes the one variable name and value have to be assigned single times only. It's unable to reassign and redecleration.



2️⃣ What is the spread operator (...)?
Ans: It's use to be release the data from inside the object and array. 


3️⃣ What is the difference between map(), filter(), and forEach()?
Ans: forEach():- It's Executes into array for looping purpose and separated the data from an array.

map(): it can modify an array as a result becomes the new array from in array element.

filter(): It can filter inside array data to comparison purpose. from the filter() becomes a new array.



4️⃣ What is an arrow function?
Ans: Arrow function is the function that can use to short function decleration system. It's released from ES6.


5️⃣ What are template literals?
Ans: Template literals means the combination of backtik and variable ${}. it can execute in JavaScript as priority basis. specially for custom html design in javascript.
